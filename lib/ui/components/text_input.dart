import 'package:flutter/material.dart';

class MyFormTextInput extends StatelessWidget {
  const MyFormTextInput({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return TextFormField();
  }
}
